/**
 * As vantagens de usar o export
 * default ta em não precisar 
 * desestruturar e ja poder por nome
 * sem utilizar o "as"
 */

export default function areaRetangulo(base: number, altura: number): number{
    return base * altura
}